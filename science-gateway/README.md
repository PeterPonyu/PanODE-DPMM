# Static site source

Next.js App Router static export used to build this repository's GitHub Pages companion.

From this directory:

```bash
npm ci
npm run build
npm run verify
```

The export describes the public code tree. It is not a journal article and must not ship manuscript figures or result tables.
